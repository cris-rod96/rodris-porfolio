---
import SectionContainer from "./SectionContainer.astro";
---

<SectionContainer title="Contacto" href={null}>
  <h3 class="text-3xl text-white mb-3 text-center">
    ¿Tienes un proyecto en mente?
  </h3>
  <h5 class="mb-10 text-center text-gray-400">
    Estoy abierto a colaborar en nuevas ideas o simplemente a conversar sobre
    desarrollo. Escríbeme y lo hablamos.
  </h5>
  <div class="grid md:grid-cols-2 grid-cols-1 gap-2 mb-3">
    <input
      type="text"
      placeholder="Nombres"
      class="bg-gray-500/10 h-[50px] px-3
         border border-gray-500/20 rounded-md text-white
         outline-none
         focus:border-[#8914dc] focus:ring-2 focus:ring-[#8914dc]/50"
    />
    <input
      type="email"
      placeholder="Correo electrónico"
      class="bg-gray-500/10 h-[50px] px-3
         border border-gray-500/20 rounded-md text-white
         outline-none
         focus:border-[#8914dc] focus:ring-2 focus:ring-[#8914dc]/50"
    />
  </div>

  <input
    type="text"
    placeholder="Asunto"
    class="bg-gray-500/10 h-[50px] px-3
         border border-gray-500/20 rounded-md text-white
         outline-none
         focus:border-[#8914dc] focus:ring-2 focus:ring-[#8914dc]/50"
  />

  <textarea
    name=""
    id=""
    placeholder="Mensaje"
    class="w-full h-[200px] mt-3 resize-none bg-gray-500/10 p-3 border border-gray-500/20 rounded-md text-white outline-none focus:border-[#8914dc] focus:ring-2 focus:ring-[#8914dc]/50"
  ></textarea>

  <button
    class="mt-3 w-full h-[50px] bg-[#8914dc] border border-gray-500/20 rounded-md font-bold cursor-pointer hover:bg-[#8914dc]/75 transition-all duration-300"
  >
    Enviar
  </button>
</SectionContainer>
